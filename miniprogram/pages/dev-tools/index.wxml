<!--pages/dev-tools/index.wxml-->
<view class="container">
  <view class="header">数据库维护工具</view>
  
  <view class="card">
    <view class="title">车辆去重</view>
    <view class="desc">
      检测数据库中 `plate` (车牌) 相同的记录。
      如果有重复，将保留“更新时间”最近的一条，自动删除其余旧数据。
    </view>
    <button type="warn" bindtap="onDeduplicateVehicles" loading="{{loading}}">
      执行去重 (按车牌)
    </button>
  </view>
  
  <view class="card">
    <view class="title">日期格式修复</view>
    <view class="desc">
      扫描所有车辆，将 `createdAt` 字段从字符串 (如 "2025-10-25") 转换为标准的数据库日期对象 (Date)，以便正确排序。
    </view>
    <button type="primary" bindtap="onFixDates" loading="{{loading}}">
      修复日期格式
    </button>
  </view>

  <view class="card danger-zone">
    <view class="title danger-text">⚠️ 删库 (按城市)</view>
    <view class="desc">
      输入城市代码 (如 suzhou)，并输入确认口令，将<text style="font-weight:bold;">永久删除</text>该城市下的所有车辆数据。
    </view>
    
    <view class="input-group">
      <view class="label">目标城市 (cityCode):</view>
      <input class="input" placeholder="cityCode" bindinput="onInputDeleteCity" value="{{delCity}}"/>
    </view>

    <view class="input-group">
      <view class="label">确认口令:</view>
      <input class="input" placeholder="are you sure?" bindinput="onInputDeleteConfirm" value="{{delConfirm}}"/>
    </view>

    <button type="warn" bindtap="onDeleteByCity" loading="{{loading}}" disabled="{{!canDelete}}">
      确认彻底删除
    </button>
  </view>

  <view class="card" style="border: 2rpx solid #1677ff; background:#e6f7ff;">
    <view class="title" style="color: #1677ff;">导出车辆记录 (CSV)</view>
    <view class="desc">
      选择城市和分公司，导出当前数据库中该节点的全量车辆记录为 CSV 格式，并复制下载链接供 PC/手机端下载。
    </view>
    
    <view class="input-group">
      <view class="label">选择城市:</view>
      <picker class="input" mode="selector" range="{{exportCities}}" range-key="name" bindchange="onExportCityChange" value="{{exportCityIndex}}">
        <view class="picker-inner">
          <text wx:if="{{exportCityIndex >= 0}}">{{exportCities[exportCityIndex].name}}</text>
          <text wx:else style="color:#999;">请选择城市...</text>
        </view>
      </picker>
    </view>

    <view class="input-group">
      <view class="label">选择分公司:</view>
      <picker class="input" mode="selector" range="{{exportBranches}}" range-key="name" bindchange="onExportBranchChange" value="{{exportBranchIndex}}" disabled="{{exportBranches.length === 0}}" style="{{exportBranches.length === 0 ? 'background:#f5f5f5;' : ''}}">
        <view class="picker-inner">
          <text wx:if="{{exportBranches.length === 0}}" style="color:#ccc;">此城市无配置的分公司</text>
          <text wx:elif="{{exportBranchIndex >= 0}}">{{exportBranches[exportBranchIndex].name}}</text>
          <text wx:else style="color:#999;">请选择分公司...</text>
        </view>
      </picker>
    </view>

    <button type="primary" bindtap="onExportCsv" loading="{{loading}}">
      生成并导出CSV
    </button>
  </view>
  <view class="card" style="border: 2rpx solid #1677ff; background:#e6f7ff;">
    <view class="title" style="color:#1677ff;">CSV 智能导入 (Upsert)</view>
    <view class="desc">
      上传 CSV 文件。系统会自动根据 <text style="font-weight:bold;">车牌号 (plate)</text> 匹配：
      <view>• 如果车牌存在 -> 更新该车辆信息 (保留历史ID)</view>
      <view>• 如果车牌不存在 -> 创建新车辆</view>
    </view>
    <button type="primary" bindtap="onImportCsvUpsert" loading="{{loading}}">
      选择文件并导入
    </button>
  </view>

  <view class="card danger-zone" style="border: 2rpx solid #fa8c16; background:#fff7e6;">
    <view class="title" style="color: #d46b08;">车牌错别字深层订正</view>
    <view class="desc">
      切勿直接在控制台修改车牌字眼！由核心底层控制该操作才能保证安全。在此替换车牌，系统会同时跨集合修正 <text style="font-weight:bold">车辆主体历史、系统时间线、以及所有历史绑定合同</text> 的底层外键映射，以免断连。
    </view>
    
    <view class="input-group">
      <view class="label">当前报错的旧车牌 (例如 粤A12344):</view>
      <input class="input" placeholder="请输入旧车牌" bindinput="onInputOldPlate" value="{{fixOldPlate}}" />
    </view>

    <view class="input-group">
      <view class="label">更新为正确的新车牌 (例如 粤A12345):</view>
      <input class="input" placeholder="请输入新车牌" bindinput="onInputNewPlate" value="{{fixNewPlate}}" />
    </view>

    <button type="primary" style="background-color: #fa8c16; color: #fff;" bindtap="onFixPlate" loading="{{loading}}" disabled="{{!fixOldPlate || !fixNewPlate}}">
      执行全库车牌安全置换
    </button>
  </view>

  <view class="card">
    <view class="title">城市转分公司数据迁移</view>
    <view class="desc">
      由于苏州和佛山从独立城市升级为多基地分公司，这会将旧数据强制归属到默认的初始分公司以防丢失展示:
      <view>• 苏州记录 -> [suz_a]</view>
      <view>• 佛山记录 -> [fos_b]</view>
    </view>
    <button type="primary" bindtap="onMigrateBranches" loading="{{loading}}">
      执行迁移找回历史数据
    </button>
  </view>

  <view class="card">
    <view class="title" style="color: #faad14;">逾期车辆收回 (Auto-Return)</view>
    <view class="desc">
      手动触发系统扫描，强制将合同已过期 (按到期日早晨 6:00 计算) 的车辆从『已租』变回『闲置』，并解绑相关司机。
    </view>
    <button type="primary" style="background-color: #faad14; color: #fff;" bindtap="onAutoReturnExpired" loading="{{loading}}">
      手动执行逾期车辆收回
    </button>
  </view>

  <view class="card" style="border: 2rpx solid #722ed1; background:#f9f0ff;">
    <view class="title" style="color:#722ed1;">线下合同 CSV 导入</view>
    <view class="desc">
      上传线下记录的 CSV 文件，强制更新司机信息与车辆已被绑定的租赁关系，同时生成不带电子签约文件的“补录”合同记录。
    </view>
    <button type="primary" style="background-color: #722ed1; color: #fff;" bindtap="onImportOfflineContracts" loading="{{loading}}">
      选择文件并执行导入
    </button>
  </view>

  </view>